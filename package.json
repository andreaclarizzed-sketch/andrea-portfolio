{
  "name": "andrea-portfolio-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "([ -n \"$TINA_CLIENT_ID\" ] && tinacms build && astro build) || astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.4.4",
    "tinacms": "^3.9.1"
  }
}